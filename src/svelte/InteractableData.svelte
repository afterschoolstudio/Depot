<script>
    import { onMount, setContext } from 'svelte';
    // import { interactableJSON } from './InteractableData.js';

    // setContext("data", {
    //     getData: () => data
    // });

    // let data = interactableJSON;
    // setContext("data", data);
    setContext("data", "tedt");


    // onMount(() => {

    // });
</script>

<!-- <svelte:window on:={handleKeydown}/> -->